#!/usr/bin/env bash
# One-command stop entry point for the durable reproduction worker.
set -euo pipefail

PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${PROJECT_ROOT}/reproduce-all" --stop
